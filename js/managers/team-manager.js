// Pour la démo, le manager ne fait rien (tout est en localStorage via team.js)
// Quand tu voudras brancher Firebase, c’est ici que tu ajoutes les méthodes CRUD.
export class TeamManager {
    // À implémenter plus tard pour Firebase (addMember, removeMember, getMembers, etc.)
}
